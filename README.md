# snaprat
file organizer

## tests
```bash
bundle exec rspec -format documentation

SnapRat
  knows the creation dates of files
  splits timestamp of files into directory structures
  can check the contents of a directory
  sorts files into directory structure of year/month/day (PENDING: Temporarily skipped with xit)
  copies files into directory structure of year/month/day (PENDING: Temporarily skipped with xit)
```
